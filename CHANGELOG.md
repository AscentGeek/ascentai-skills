# 변경 이력

이 문서는 **지난 배포본과 무엇이 달라졌는지**를 인수 담당자가 한눈에 보기 위한 기록입니다.
스킬 사용법은 각 스킬의 `SKILL.md` 와 `references/` 를, 저장소 구조와 빌드는 `AGENTS.md` 를 보세요.

---

## 2.0.0

### 한 줄 요약

국가별로 나뉘어 있던 스킬 8개(kr·jp)를 **스킬 4개로 합쳤습니다.** 프롬프트는 영어 한 벌이고,
**분석할 시장과 리포트를 쓸 언어를 실행할 때 각각 지정**합니다.

### ⚠ 호환성 깨짐 — 인수 시 반드시 확인

| 항목 | 1.0.0 | 2.0.0 |
|---|---|---|
| 스킬 이름 | `lm-queryfinder-report-kr` · `-jp` | `lm-queryfinder-report` (국가 코드 없음) |
| zip 개수 | 스킬 4 × 언어 2 = 8개 | 4개 |
| 렌더 인자 | `--gl <국가>` 하나 | `--gl <시장>` + `--lang <리포트 언어>` |
| 예시 경로 | `examples/<언어>/<스킬명>.html` | `examples/<스킬명>.html` |

### 1. 시장과 리포트 언어를 분리했습니다

실행할 때 두 값을 **따로** 받습니다. 서로 독립이라 어떤 조합도 가능합니다(3 × 3 = 9가지).

- **타겟 시장 `--gl`** — 어느 검색 시장을 분석할지 · `kr` `jp` `us`
- **리포트 언어 `--lang`** — 리포트와 대화를 어느 언어로 쓸지 · `kr` `jp` `us`

예를 들어 `--gl jp --lang kr` 이면 **일본 시장을 분석해 한국어 리포트**를 냅니다.
`--lang` 을 생략하면 시장 언어를 따라가므로, 기존 방식대로 `--gl` 만 줘도 동작합니다.

라벨(리포트 UI 문구)은 `--lang` 으로 고르고, 표지의 시장명은 `--gl` 로 고릅니다.
스킬 zip 에는 **세 언어 라벨이 모두** 들어 있습니다.

### 2. 검색어 번역 버튼이 생겼습니다 — ⚠ 존치 여부 검토 필요

> **이 기능은 DaaS 판에 없습니다.** 시장과 리포트 언어를 분리하고 보니 검색어만 읽을 수
> 없는 언어로 남아 필요해 보여 이번에 새로 넣은 것입니다. **넣을지 뺄지 검토해 주세요.**
> 빼기로 하면 렌더러의 `--translations` 인자와 `keywords` 서브커맨드, 각 `references/` 의
> Step 5.5 를 걷어내면 됩니다 — 다른 기능과 얽혀 있지 않습니다.

시장 언어와 리포트 언어가 다르면 리포트 상단에 **검색어 번역** 버튼이 나옵니다.
누르면 화면의 검색어가 리포트 언어로 바뀌고, 다시 누르면 원문으로 돌아옵니다.

글자를 JS 로 갈아끼우지 않고 CSS 로 전환하므로 **A4 전환과 인쇄에서도 상태가 유지**됩니다.
번역은 분석 단계에서 LLM 이 만들어 `--translations` 로 넘깁니다(각 `references/` 의 Step 5.5).
두 언어가 같으면 버튼 자체가 나오지 않습니다.

> **존치 검토 시 참고** · 번역 대상 키워드 수가 스킬마다 크게 다릅니다.
> pathfinder 38개, queryfinder 135개, **clusterfinder 773개**(그중 716개는 검색량 뱃지를
> 눌러야 보이는 목록). 클러스터·통합 리포트는 매 실행마다 LLM 이 수백 개를 번역해야 해
> 시간과 토큰 부담이 큽니다. 남긴다면 전량 번역할지, 항상 보이는 것만 할지도 함께 정해야
> 합니다(항상 보이는 것만 하면 clusterfinder 기준 773개 → 57개).

### 3. 의도 유형 배지를 데이터로 계산합니다

전에는 LLM 이 쓴 자유 문구를 그대로 띄워서, 라벨에 없는 값이 영어 키(`intentType`)로
새어 나오곤 했습니다. 이제 키워드마다 딸려 오는 **의도 비율(i/n/c/t)을 검색량으로 가중
합산**해 대표 의도를 코드가 정합니다. 쏠린 시장에서도 구분되도록 비중을 함께 표시합니다.

실데이터 예시 — `립 색상 선택 거래 92%` · `브랜드명 상업조사 99%` · `성분 안전 정보탐색 72%`

### 4. 브랜드/논브랜드를 의도 유형에서 떼어냈습니다

브랜드·논브랜드는 검색어의 종류지 의도가 아닌데 "의도 유형" 자리에 들어가 있었습니다.
**검색어 유형**(`Query Type` · `検索語の種別`) 라벨로 분리했고, 브랜드 그룹도 데이터 기반
의도 배지를 함께 답니다.

### 5. 빈 섹션이 사라졌습니다

브랜드/논브랜드 카드에는 판단 요인(KBF)이 없어 **제목만 있고 내용이 빈 칸**이 카드마다
남았습니다(리포트당 10개). 내용이 없으면 섹션을 통째로 생략합니다.

### 6. 저장소 구조를 정리했습니다

- **로케일 폴더 제거** — `skills/<스킬>/locales/<언어>/` 가 없어지고 `SKILL.md` ·
  `references/` · `labels/` 가 스킬 바로 아래로 올라왔습니다. 라벨만 언어별 3벌입니다.
- **total-report 의 코드 복제 제거** — 형제 스킬과 바이트 동일하던 집계기·컴포넌트·템플릿·
  스타일·라벨·문서를 지우고, `skill.yaml` 의 `borrows` 로 선언해 빌드가 가져옵니다.
  **버그를 한 곳만 고치면 됩니다.**
- **운영 프롬프트 스냅샷(`prompts/`)을 zip 에서 제외** — 실행에 쓰이지 않고 유지보수자가
  대조할 때만 보는 파일이라 저장소에만 둡니다.
- **예시 평탄화** — `examples/<스킬명>.html`

### 7. 그 밖에 고친 것

- 여정 흐름도 노드가 번역에서 빠지던 문제 — 흐름도는 `flowTree` 로 그려지는데 추출이
  `lm_paths.json` 만 봤습니다. `keywords --context` 로 해결.
- 통합 리포트의 매트릭스 셀·허브 행·누수 바·회랑·백로그 칩이 번역되지 않던 문제(8개 지점).
- 목적 문구에서 번역 span 이 이중 이스케이프되던 문제.
- 표지의 씨드 검색어가 번역 대상에서 빠지던 문제.
- 통합 리포트 매트릭스의 배지가 셀을 넘어 옆 칸을 덮던 문제(영어 라벨이 길어서).
- 존재하지 않는 `emit_turn.py` 를 가리키던 안내·경고 문구.
- 영어 라벨 어순(`Search intents 5` → `5 search intents`)과 대소문자 통일.

### 검증

4개 스킬 × 시장 3 × 리포트 언어 3 = **36개 조합 전부 실데이터로 렌더 확인**했습니다.

| 스킬 | 시드 | 시장 → 리포트 언어 | 데이터 규모 |
|---|---|---|---|
| clusterfinder | 단백질 쉐이크 | kr → 영어 | 22클러스터 · 777키워드 · 987엣지 |
| pathfinder | カルビー | jp → 한국어 | 300경로 · 217노드 · 45허브 |
| total | 롬앤 | kr → 영어 | 3파인더 통합 · 커버리지 25% |

수집부터 렌더까지 `references/` 문서의 단계를 그대로 밟았고, 브라우저에서 화면도 확인했습니다.

### 알려진 제약

- **DaaS 판(`lm-*-daas`)과는 별개 저장소**이며 이 변경은 반영돼 있지 않습니다.
- 대표 키워드 줄 끝의 `외 N건`(`+N more`) 칩은 **눌러도 동작하지 않습니다.** 전체 목록은
  카드 머리의 검색량 뱃지를 눌러 봅니다. DaaS 판과 동일한 동작이라 그대로 두었습니다.
- `SKILL.md` frontmatter 의 `locale` 은 `us` 로 고정입니다. 프롬프트가 영어라는 뜻이며,
  리포트 언어와는 무관합니다. admin 이 `kr|jp|us` 만 받기 때문입니다.

---

## 1.0.0

ListeningMind MCP 기반 리포트 스킬 첫 배포. 국가별 판(kr·jp)으로 나뉘어 있었습니다.
